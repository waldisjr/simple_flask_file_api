description: Bad Request
content:
  application/json:
    schema:
      properties:
        result:
         type: boolean
        message:
          type: string
    example:
      result: False
      message: "Incorrect request structure. Check documentation."